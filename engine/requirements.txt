opencv-python>=4.8.0
numpy>=1.24.0
websockets>=11.0
mediapipe>=0.10.0
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
pytesseract>=0.3.10
requests>=2.31.0
# Tracking — SORT & DeepSORT
filterpy>=1.4.5
scipy>=1.10.0
scikit-image>=0.21.0
trimesh>=4.0.0
deep-sort-realtime>=1.3.2
librosa>=0.10.0
# Pin a modern numba so the resolver doesn't backtrack to numba 0.53.1, whose
# llvmlite 0.36 has no Python 3.12 wheel and fails to build from source (it caps
# Python <3.10). numba>=0.60 ships py3.12 wheels and matches numpy>=1.24.
numba>=0.60.0
soundfile>=0.12.0
sounddevice>=0.4.6
# GIS / Remote Sensing
rasterio>=1.3.0
fiona>=1.9.0
pyproj>=3.5.0
shapely>=2.0.0
earthengine-api>=0.1.390
geopy>=2.4.0
# AI Models
huggingface_hub>=0.20.0
transformers>=4.44.0
timm
typing_extensions>=4.0
SAM-2 @ git+https://github.com/facebookresearch/sam2.git
# Symbolic Regression / Genetic Programming
gplearn>=0.4.2
scikit-learn>=1.3.0
pandas>=2.0.0
joblib>=1.3.0
# Plotting & Imaging (used by ML/visualize nodes)
matplotlib>=3.7.0
Pillow>=10.0.0
# OCR
easyocr>=1.7.0
# Deep Learning trainer (TD6 U-Net, autoencoder TDs)
pytorch-lightning>=2.0.0
# NetCDF / Oceanography TDs (TD1–6)
xarray>=2023.1.0
netCDF4>=1.6.0

# ── Optional / heavy (auto-installed on demand via ensure_packages) ──
# Uncomment to bundle into the build for offline use.
# sentinelhub>=3.9.0
# pystac-client>=0.7.0
# planetary-computer>=1.0.0
# odc-stac>=0.3.0
# copernicusmarine>=1.0.0
# diffusers>=0.27.0
# pyserial>=3.5
